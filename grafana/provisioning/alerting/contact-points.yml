apiVersion: 1

contactPoints:
  - orgId: 1
    name: Slack Fraud Alerts
    receivers:
      - uid: slack-fraud-alerts
        type: webhook
        settings:
          url: "${SLACK_WEBHOOK_URL}"
          httpMethod: POST
        disableResolveMessage: false